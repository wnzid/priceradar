const UA = "PriceRadar/1.0 (+https://github.com/anthony/priceradar; promotion aggregator)";
export async function fetchText(url: string, attempts = 3): Promise<string> { let last: unknown; for (let i=0;i<attempts;i++) { try { const response = await fetch(url, { headers: { "user-agent": UA, accept: "text/html,application/json" }, signal: AbortSignal.timeout(20_000) }); if (!response.ok) throw new Error(`${response.status} ${response.statusText}`); return response.text(); } catch (e) { last=e; if (i < attempts-1) await new Promise(r => setTimeout(r, 750 * 2 ** i)); } } throw last; }
export async function fetchBytes(url:string):Promise<Uint8Array>{const response=await fetch(url,{headers:{"user-agent":UA,accept:"application/pdf"},signal:AbortSignal.timeout(60_000)});if(!response.ok)throw new Error(`${response.status} ${response.statusText}`);return new Uint8Array(await response.arrayBuffer())}
