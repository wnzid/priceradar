import {readFile} from "node:fs/promises";import {describe,expect,it,vi} from "vitest";import {collectBarboraPages,parseBarboraPage} from "../scripts/collectors/barbora.js";
const fixture=()=>readFile(new URL("./fixtures/barbora-aciu.html",import.meta.url),"utf8");
describe("Barbora AČIŪ collector",()=>{it("keeps the explicit campaign promotion and rejects an ordinary product",async()=>{const parsed=parseBarboraPage(await fixture(),"https://barbora.lt/aciu-akcijos?page=1");expect(parsed).toMatchObject({rawCount:2,lastPage:2});expect(parsed.offers).toEqual([expect.objectContaining({externalId:"barbora-12345",salePrice:3.65,loyaltyPrice:3.65,regularPrice:4.99,pricePerUnit:3.65,pricePerUnitType:"kg",sourcePlatform:"barbora",channel:"online",promotionType:"LOYALTY"})])});it("paginates completely and deduplicates stable IDs",async()=>{const html=await fixture(),fetcher=vi.fn(async()=>html);expect(await collectBarboraPages(fetcher)).toHaveLength(1);expect(fetcher).toHaveBeenCalledTimes(2)});it("rejects challenge pages",()=>expect(()=>parseBarboraPage("<title>Just a moment...</title><div id='challenge-error-text'>wait</div>","https://barbora.lt/aciu-akcijos")).toThrow(/Cloudflare/))});
